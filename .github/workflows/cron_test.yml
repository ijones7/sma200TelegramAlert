name: Cron Test

on:
  schedule:
    - cron: '0 0 1 * *'      # jede Minute (UTC), nur testweise
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Tick
        run: |
          date -u
          echo "cron tick (UTC)"
